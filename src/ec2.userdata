#!/bin/bash
sudo yum update -y
sudo amazon-linux-extras install nginx1 -y
sudo systemctl enable nginx
sudo systemctl start nginx
echo "<h1>Welcome to Nginx Web Server in Auto Scaling Group</h1>" | sudo tee /usr/share/nginx/html/index.html
